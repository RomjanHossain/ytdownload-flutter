import 'package:flutter/material.dart';

/// channel vids
class ChannelVideos extends StatelessWidget {
  /// constractor
  const ChannelVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
